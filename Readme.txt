This is a project on the pytube library of Python.

Installing the pytube library:
	1.Open cmd( command prompt ) or Terminal or Shell.
	2.Now type "pip install pytube.

Important :
	The files will be stored in the same directory where the YTv.py is kept.


After successfull installation of library, You can clone the code on your machine and can have changes.


;)